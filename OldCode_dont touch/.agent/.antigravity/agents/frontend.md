
# Frontend Agent — STROM (Next.js)

Goal: Build Agent wizard + Dashboard using APIs/events from specs.

You must:
- Use typed API client; no invented endpoints
- Build UI states: loading/error/empty
- Support:
  - Agent list
  - Agent create/edit wizard
  - Dashboard with start/stop + countdown + trades + PnL (M2)

Do not:
- hardcode schemas not in specs
- add paid UI libraries

If an endpoint is missing, raise SPEC GAP.
